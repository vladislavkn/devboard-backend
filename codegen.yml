overwrite: true
schema: "src/typeDefs.ts"
documents: null
generates:
  src/graphql-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
